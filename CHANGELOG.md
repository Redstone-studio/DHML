# 更新日志

发布时，workflow 会从这份文件里抓**版本号完全相同**的那一节，当作 GitHub Release
的发行说明。所以发版前，先在这里加一段。

- 标题格式必须是 `## vX.Y.Z` 或 `## vX.Y.Z-后缀`，要和发布时填的版本号**一模一样**
- 内容写到下一个 `## ` 标题为止
- 找不到对应版本也不会让发版失败，只会在发行说明里写一句提示
- 发行说明末尾会自动附上「与上一个 tag 的完整变更」链接

---

## v0.1.6-alpha

- 界面文字全部走文案系统，支持中英文实时切换（设置页 → 界面语言）
- 设置页内容比窗口高时可以滚动，不再出现卡片被压扁/切掉
- 账户列表改成卡片行：图标 + 名字 + uuid + 类型标签，当前账户额外标注
- 修复版本页脚注永远为空的问题
- 版本选择器的箭头、勾选框的对勾改用 SVG 图标（之前用 CSS 技巧画，Qt 不支持，出来是方块）
- 侧边栏背景色、勾选框文字底色不再串色
- 打包后的 exe 终于会加载深色主题了（之前路径解析错误，异常还被静默吞掉）
