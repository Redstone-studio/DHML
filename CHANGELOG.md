# 更新日志

发布时，workflow 会从这份文件里抓一节当 Release 正文，按这个顺序找：

1. 标题和版本号**完全相同**的（`## v0.1.6-alpha`）
2. `## 未发布` 或 `## Unreleased`

所以**平时改完东西，就往「未发布」里追加一行**；发版时把它改成版本号，
忘了改也没关系 —— 会自动用「未发布」那一节。

节的写法：标题必须是 `## ` 开头，内容写到下一个 `## ` 为止。
两节都找不到也不会让发版失败，只在正文里写一句提示。

⚠️ **改这份文件别用网页编辑器把整段粘进去**：Markdown 的 `## ` 和行首的 `- `
一丢，workflow 就找不到这一节（v0.5.6 那次就这么坏过一回），
而且那一节的排版会糊成一大坨。要改就改**某一行**，或者整份文件覆盖上传。

---

<!-- 改了东西就往下面那一节里加一行。发版时把「未发布」这个标题改成版本号；
     忘了改也没关系 —— workflow 找不到同名版本时会自动用「未发布」那一节。
     ⚠️ 说明文字和 HTML 注释都放在标题**上面**：workflow 抓的是「从标题到下一个 ## 」，
     写在标题下面的注释会被原样塞进 Release 正文里（v0.5.6 的正文里就混进去过一段）。 -->

## 未发布

- 下一版还没开始攒改动；**v1.0.0（第一个正式版）**的内容见下面那一节

## v1.0.0

- **第一个正式版**（之前一直挂着 `-alpha`，功能其实早就齐了）：版本号提到
  `v1.0.0`，workflow 里那个要手填的版本号默认值也跟着改了

- **去掉了社区资源页顶部那排类型标签**（用户 2026-09 要的）：左栏「社区资源」
  下面本来就是模组 / 整合包 / 数据包 / 资源包 / 光影包，顶上再摆一模一样的
  一排属于重复入口。切栏走的还是同一个函数，功能没少；「现在搜的是哪一类」
  由面板标题和搜索按钮显示
- **关于页补上了仓库地址**（`https://github.com/kongxia114/Mosslight-Launcher`）：
  之前留着空，所以「查看源代码」一直是禁用 + "链接待定"；顺带把产品简介改写成
  现在真实的样子（原文还写着"启动游戏和版本下载还在做"）

- **动效可以整个关掉了**（用户 2026-09 要的："能节省性能"）。动效设置里多一个
  「**关闭动效（省性能）**」勾选框：关掉之后列表**直接出现**，不滑入也不淡入。
  省的不是"把时长改成 0"，而是**那些开销根本不创建**：
  · 列表**连动画层都不包**（少一个控件、少一个定时器、少一次属性动画）
  · **不挂 `QGraphicsOpacityEffect`** —— 每张卡片一次离屏合成，这是整条链路上
    最贵的一环，淡入那个开关也一并交它接管
  · 切页不再整页位移（那个动画每帧要重画整页），可收起区块也不播高度动画
  关掉时勾选框下面会写明现在是"已关闭"，风格 / 速度那些选项置灰但**都留着**，
  取消勾选就恢复原样（不会"关一次就再也回不来"）

- **修好"某些地方间距莫名其妙变大"**（用户 2026-09 照截图报的）：入场动画那层
  （`SlideInRow`）把自己的高度**照卡片 `sizeHint()` 算**，而卡片常常自己
  `setFixedHeight()` 过（搜索结果卡是 96）—— 它那个 `sizeHint()` 会跟着里面描述
  文字的长短在 **94~122** 之间飘：飘大了每行就多出一截空白，飘小了卡片底部被
  切掉。现在按卡片**真正能占的高度**（夹进它的 min/max）来定，一行 96 就是 96
- **列表行加回了底板**（用户 2026-09 第二次反馈，和第一次是反的 —— 不是谁改错了）：
  折叠组展开出来的版本行原来是"扁平无框"的（当时明确要的，没背景图时也确实更清爽），
  但**设了背景图之后行是直接浮在图上的**：文字和图标跟背景糊在一起读不清。
  现在照实验项目的做法给每行一块底色（它那边本来就是每行一张卡），
  底色比标题栏暗一级，行距也跟着放大
- **左侧竖栏和下载页那列分类栏会半透明**（用户 2026-09 要的："左侧要变成半透明"）：
  设了背景图时它们用 **82%** 不透明的底色 —— 图能透出来、字仍然清楚。
  ⚠️ 它们**不听"卡片透明度"那一档**：那一档是给内容卡片的，用户完全可以
  把卡片留在实心而仍然希望侧栏透出背景；也只在**设了背景图**时才这么做
  （没图时半透明只会跟页面糊成一片，纯色背景下透出来是同一种颜色、等于把层次抹掉）

- **背景 + 入场动效整套搬过来了**（用户 2026-09 要的："最好能完美移植过来"，
  原型是 `experiments/Downloading mod test`，那边已经调得很顺）：
  · **背景**：背景图（填充 / 适应 / 拉伸 / 居中 / 跨区 五种铺法）、纯色底色、
    **压暗**滑块（亮图会把卡片和文字冲得看不清）、**卡片透明度**（只透卡片底色，
    文字和图标不受影响），铺满整窗的画布在最底下 —— 页面底色改成透明的，
    背景才透得出来
  · **入场动效**：列表卡片依次滑入，7 档预设（左侧滑入 / 弹簧 / 过冲 18% /
    下方滑入 / 弹性 / 仿 PCL2 克制 / **PCL 原味**（位移拆成快、慢两条并行曲线 +
    同时淡入）），5 档速度**整体**缩放（时长和错峰一起缩 —— 只放慢单张会变成
    一张张慢慢蹦），切换页面也带滑入
  · 入口是**个性化页**新增的「**背景与动效**」卡片（两个按钮开子界面），
    改完立刻生效、不用重启
- ⚠️ 移植时发现**实验里那套滑入在 Qt 6.11 上其实一直没生效**：它靠"给容器布局设
  **负内边距**"把卡片推到屏幕外，而 `QLayout.setContentsMargins()` 收到负数会
  **退回样式默认值**（设 -120，读回来是 11），卡片一步都不会动 ——
  没人盯着逐帧看就一直没发现。现在改成这一层**不建布局**、直接 `move()` 孩子，
  动效才真的看得见（这条和实验项目的做法**故意不一样**，见 `slide_in.py` 的注释）
- 可收起区块（背景设置里「多线程下载」那一块）的标题栏**以前在代码里写死了深色**
  （控件自己的样式表优先级高于 app.qss，从外面盖不掉），浅色主题下会变成一根
  深色横条；现在走主题变量

- **修好了一个"整合包本体下好了、但加载器装不上"的真故障**（用户 2026-09 真机上
  撞的，包是 Forge 1.20.1 / 47.4.10）：Forge 安装器要自己下它那 36 个库，而
  `maven.minecraftforge.net` 把 `guava-25.1-jre.jar` **下到一半掐断**（1.72 MB / 2.61 MB，
  sha1 对不上），它重试几次后直接放弃：`These libraries failed to download.`。
  现在**跑安装器之前先替它把这批库下好**（读安装器 jar 里的 `install_profile.json`，
  用我们自己的引擎下到 `libraries\`：有 BMCLAPI 镜像、有重试、**认 sha1**）——
  安装器看到 `File exists: Checksum validated.` 就直接跳过，那批网络根本不会再走
- 修好**重跑那条路**：上次装到一半失败会留下安装器自己命名的目录
  （`1.20.1-forge-47.4.10`），而我们的代码只认"这次新出现的目录" → 重跑时会说
  「安装器跑完了，但没看到新的版本目录」白跑一趟。现在也认"安装器那套命名"的既有目录，
  而且**按加载器和版本号认**（NeoForge 1.20.1 那代也叫 `forge-47.1.x`，
  只看名字会把正牌 Forge 错认成 NeoForge）
- 安装器超时 30 分钟 → **2 小时**（网络抽风时别被我们自己掐死；正常 200 秒就完）
- **下载任务支持"备用地址"**：主地址挂了会一路试下去（镜像 → 官方 → 备用）。
  起因是真跑 Forge 时撞上的：**68 个文件里只有那个安装器 jar 挂了**
  （BMCLAPI 自己的 `/forge/download?...` 接口返回 **403**），而整个 Forge 安装
  就靠这一个文件 —— 它一挂加载器就装不了。现在 Forge 的安装器多配了一个
  官方 maven 的备用地址（老版本 `1.7.10-10.13.4.1614-1.7.10` 那种坐标也对）
- **一个版本 = 一个独立文件夹**（用户 2026-09 要的："分多版本不好管理，独立版本
  目录方便管理"）。以前是两层（`versions/1.20.1/` 放原版、`versions/1.20.1-Forge_47.4.0/`
  只放加载器那份 JSON，靠 `inheritsFrom` 去原版那层拿 jar —— PCL 也是这么干的），
  装一个加载器版本列表里就多一行。现在装完会**把两层合起来**：
  加载器那份 JSON 写成**自包含**的（原版的 mainClass / 库 / 参数 / 资源索引全合进去，
  去掉 `inheritsFrom`），客户端 jar 放进它自己的目录（**硬链接**指向那份原版，
  不额外占盘），原版那层收进 `<游戏目录>/.mosslight/vanilla/` **藏起来但不删** ——
  以后装同一个游戏版本的别的加载器会自动搬回来，不用重下那 25 MB。
  ⚠️ **用户自己装的原版、以及还有版本继承着它的，一个字都不动**（见
  `core/standalone.py` 的 `cleanup_vanilla`）；老的带 `inheritsFrom` 的版本照旧能启动。
  ⚠️ 合并时**我们自己写进去的字段会原样搬过去**（整合包那份 `modpack` 元数据、
  `releaseTime` 那种发布时间）—— `load_version()` 只合固定那几项，不管的话
  合并这一步会**悄悄把整合包信息抹掉**（真装 Forge 整合包时才发现的）
- 整合包也一起合了：装完列表里只剩**包名这一行**（以前是「原版 + 加载器 + 我的包」
  三行，找自己那个包都费劲），中间那层加载器目录合并之后撤掉
- 下载**失败的文件自动再试一轮**：以前 68 个文件里挂 1 个，加载器就不装了，
  用户只能点下载窗口那个「重试失败的文件」—— 而那时后台线程早跑完，
  **重试也不会再跑安装器**（真跑 Forge 时踩到的）
- **加载器不预选了**（用户 2026-09 定的："让用户自己选择，不展开任何选项"）：
  安装选项页一进来五组**全是收起的**，谁都不默认展开、也没有默认加载器 ——
  **一个都不点就只装原版**（按钮上方会明说这句话）。以前是"什么都不点就装
  第一个能装的（Forge）"，等于替用户做主、而且界面上看不出来。
  想装加载器就自己展开一家、点里面一个版本
- **点了「开始下载」就自动退回版本列表**（用户 2026-09 要的）：装一个版本要几分钟
  （Forge 那种还要跑安装器），把用户按在「安装选项」那一页上没法改选、也看不到
  别的版本。现在任务一跑起来就退回列表，进度交给那个下载窗口，
  「正在装哪个版本」写在列表页的状态行上（装完换成结果，不再挂着「正在安装…」）；
  正在装的时候再点一次也会把这句话写出来，不会像以前那样"点了没反应"
- **Forge / NeoForge / OptiFine 也能一键装了**（启动器自己跑官方安装器）：
  下完安装器 jar → 摆好 `launcher_profiles.json`（缺了它 Forge 会直接报
  「There is no minecraft launcher profile」）→ 挑一个够版本的 Java 跑安装器 →
  校验不是半成品（`BUGS.md` BUG-04）→ 把版本目录改成 PCL 那套名字
  （`1.20.1-Forge_47.4.0` / `1.20.1-OptiFine_HD_U_I5`）。
  实测：Forge 1.20.1 约 200 秒、NeoForge 1.20.1 约 153 秒、OptiFine 约 8 秒
- 安装器跑起来的时候，下载窗口**下半截会多一块控制台**：把安装器的输出
  节流后铺出来（Forge 一次刷 3 万多行，不节流会把界面刷死），进度条切成
  「不知道还要多久」的滚动状态。纯下载的安装（原版 / Fabric）看不到这块
- ⚠️ OptiFine 那个 jar **不能用 `java -jar`**：它的 MANIFEST 里
  `Main-Class` 是 `optifine.InstallerFrame`（一个要人点「Install」的窗口）。
  同一个 jar 里另有无窗口入口 `optifine.Installer`，走 `-cp <jar> optifine.Installer`
  才是真正的一键装 —— 不然用户会莫名其妙看到一个安装器窗口
- ⚠️ 修了一个**会静悄悄弄坏 OptiFine 的启动 bug**：OptiFine 的版本 JSON 是
  `inheritsFrom: 1.20.1` 且**没有** jar 字段，但它自己目录里躺着 23 MB 的
  打过补丁的客户端。合继承链时只认 JSON 字段的话会退回原版 jar ——
  表现是"进游戏没有光影"，一声不响。现在按官方启动器的规矩：
  **自己目录里的 `<目录名>.jar` 优先**（JSON 里显式写了 `jar` 的照样听它的）
- 改名那一步现在**连 `<旧名>.jar` 一起改**（OptiFine 的补丁 jar 就在目录里），
  不然改完名字启动会去找一个不存在的 jar
- NeoForge 1.20.1 那批在 maven 里的**产物版本号带游戏版本前缀**
  （`net/neoforged/forge/1.20.1-47.1.106/`），不带前缀是 404 —— 按版本号自己认
- 安装器 jar 落在 `<游戏目录>/.mosslight/installers/`（不自动删：装失败时
  能翻出来手动重跑，重试也不用重下）
- 整合包那条路也接上了：包里声明的 Forge / NeoForge 会装进**它自己**的版本目录
  （`1.20.1-Forge_47.4.0`），实例目录写一份 `inheritsFrom` 指过去的 JSON ——
  实例 → 加载器 → 原版三层，`load_version()` 的 jar 回退正好一路找到原版客户端
- **社区资源五栏全接上了**：模组 / 整合包 / 数据包 / 资源包 / 光影包都走同一个页面
  （搜索 + 详情一页两态），左栏点哪栏就是哪个类型。「世界」还是占位 ——
  Modrinth 根本没有"存档"这个类型，要做只能另找数据源
- **模组搜索 / 详情 / 下载**（Modrinth；数据层 `core/modrinth_api.py`，纯 Python 不碰 Qt）：
  关键词 + 游戏版本 + 加载器 + 分类筛选，按相关度 / 下载量 / 更新时间排序。
  取数走镜像 + 重试，但 **404 不重试** —— 那表示"没这个 mod"，重试没用
- 详情页的版本**按「游戏版本 + 加载器 + 是否预览版」分组**，新的在前、正式版优先；
  标题就是 `Fabric 26w14a 预览版`（预览版**不带括号**，带括号看着像版本名的一部分）。
  在搜索页选过的筛选条件**会置顶并自动展开**那一组，标题上标「匹配你选的版本」
- 版本分组**可折叠 + 分批铺**：Fabric API 有 400+ 个分组，一次铺 8 组、滚到底自动续。
  折叠标题的最小高度（36px）、"内容不够一屏就自动补一批"、"铺设期间挡住滚动回调"
  这三条**一条都不能少**，少一条就退化成"只剩一个要点一辈子的按钮"
- **整合包（`.mrpack`）能装了**：下 `.mrpack` → 解析 → 下里面几百个文件 →
  解压 `overrides/` → 装原版和加载器 → 写版本 JSON，全在同一个进度窗口里
  （能看进度、能取消、失败能重试）。一个整合包一个实例目录（版本隔离）
- 整合包装完**能直接启动了**：以前只把包里的文件铺好，实例是"能列出来、点启动缺
  mainClass"。现在照 PCL 的做法，在包文件之后补一道"装原版 + 装加载器"，
  版本 JSON 变成一份真能启动的 profile（`inheritsFrom` 指原版、主类是加载器的），
  整合包元数据（包名 / 要哪个原版 / 什么加载器 / 来源）也都还在里面
- 整合包**外面套了一层文件夹也认**（自己重打包、或"解压完再压一遍"就会多一层，
  `overrides/` 也跟着在那一层）；装完还会**把包的图标设成实例的图标**
- **模组加载器五家全接上了**（Forge / NeoForge / Fabric / Quilt / OptiFine 的版本列表）。
  「能装 / 这个版本没有这一家 / 拉不到」三种状态分开说：
  `count == 0` 是这个游戏版本确实还没出，**不是错误**，也不说成"还没做"
- **启动器能自己装加载器了**：Fabric / Quilt 有干净的 profile JSON，
  一个 GET 拿到启动器要的版本 JSON、把库下下来就行（**不用 Java**）；
  Forge / NeoForge / OptiFine 只有安装器 jar，得跑官方安装器
  （见上面那条，现在也能装了）
- 安装选项页的**加载器互斥**：同时只能选一个，选了之后其他几家灰掉、**标题点不动**、
  写「与 Fabric 不兼容」。要换得点选中那组右边的「**取消选择**」——
  刻意**不**做成"点一下灰卡片就换"：那样用户想展开看看里面有什么，
  会静悄悄把加载器换掉
- 版本名**自动填成 `1.20.4-Fabric 0.19.5`**（跟 PCL 的目录命名一致，
  `core/mc_dir.py` 就是靠它反查"这个版本的 mod 该装到哪个目录"）；
  用户手改过就不再自动填。顶部那个图标也跟着选中的加载器变
- **选了 Fabric 会多出一组「Fabric API」**：默认装匹配当前游戏版本的最新版，
  可以改选旧版、也可以点「不装」；装进版本隔离的 `mods/`。
  数据只在**真的选了 Fabric** 之后才去 Modrinth 拉（选 Forge 的人一个请求都不多花）
- **启动器名字进游戏主界面**：1.20.4 那行字的原版逻辑是
  `"Minecraft " + 版本名 + (--versionType 不是 release ? "/" + --versionType : "")`，
  所以启动器能控制的就是 `--versionType`（版本设置里那个「自定义信息」）。
  留空时自动填成 `Mosslight/Fabric（113 个模组）`，游戏里于是显示
  `Minecraft 1.20.4/Mosslight/Fabric（已修改）`；想关掉就把它填成 `release`
  （游戏那边只有"不是 release"才接）。
  ⚠️ 中间试过"另写一个只含 `version.json` 的小 jar、插在 classpath 最前面"那条路，
  **Fabric 下不生效**：Fabric 的 `KnotClassLoader` 自己重写了资源查找，走它自己的
  jar 集合，系统 classpath 上那份轮不到；改游戏 jar 又会跟自己的 sha1 校验打架，已放弃
- 修掉几个真 bug：
  - 下载引擎的 `start()` 原来是 `if self._started: return`，**第二次 start() 什么都不做** ——
    中途 add 进来的任务永远停在「等待」，而且不报错（整合包正好是"两批任务"，卡在第二步）
  - 加载条用 `isVisible()` 判"有没有在显示"，**父页面还没切过来时它恒为 False**，
    于是数据早就到了也不收 —— 用户翻到那页看到的是一条永远转下去的加载条
  - 折叠组里已经建出来的版本行，竖向能被压扁 / 拉高（现在给 Fixed）
  - `icon` / `custom_info` 那几个字段写进 `versions.json` 会被 `_normalize()` 静默丢掉
- 文案涨到 **559 条 × 5 语言**（`extract_strings.py` 报 559 = 559，`verify_lang` 全过）

## v0.5.6-alpha

- **改名了**：界面里叫 **Mosslight**（侧边栏左上角、关于页），Windows 那边看到的完整名是
  **Mosslight Launcher**（窗口标题、任务栏、Alt+Tab）。构建产物跟着变成
  `Mosslight-Launcher.exe` / `Mosslight-Launcher-<版本>-win64.zip`
- 接上程序图标：任务栏、窗口、exe 都用自己那套图标（32/64/128/512 四档，Windows 自己挑）
- 版本列表的图标调大了一圈（38 → 44 像素）并按屏幕缩放比出图，
  高分屏（125% / 150%）下不再发软
- 版本列表左边的类型图标换成了 PCL 那套（正式版草方块 / 快照命令方块 / 远古圆石 /
  Forge 铁砧 / Fabric 线轴 / NeoForge 狐狸 / Quilt 拼布 / OptiFine / LabyMod / Cleanroom），
  源图 64×64 缩到显示尺寸几乎不重采样，比之前那批 256 的更清楚
- 版本列表左边能显示**版本类型图标**（正式版草方块、远古圆石、Forge 铁砧、Fabric 织布机、
  NeoForge 下界合金锭、Quilt 羊毛、OptiFine 望远镜、整合包箱子）；没放图的版本自动退回文字徽章
- **可以真正启动游戏了**：自动挑 Java、拼启动参数、实时显示游戏日志，随时停止
- 修复离线 UUID 算错的问题（旧值少了版本位，会让存档和服务器认不出同一个玩家）
- 首页改成**左边版本列表 + 右边启动面板**：版本下拉框去掉了（它看不见加载器类型、
  Java、能不能启动，而这些恰恰是选版本时要看的），双击版本行直接启动，
  也会记住上次启动的版本
- 侧边栏「版本」页改成**版本管理**：和首页共用同一个列表，右边显示版本路径、缺哪些库，
  可以一键补全缺失的库、打开版本文件夹
- 新增**版本独立设置**：Java、内存、额外 JVM 参数可以按版本单独定，没勾的项跟着启动器
  默认走（以后改全局默认它们会跟着变，不会被抄死的旧值卡住）。
  设置存在 `versions.json` 里，**不写进版本 JSON** —— 那个文件是安装器管的、带校验，
  改了会被当成"版本被改坏"
- 设置页的**最大内存改成滑块**（拖比点小箭头快，滑块长度本身也提示「物理内存有多大」），
  拖出来的值会吸附到 512 的档位；上限跟着物理内存走，配不出比内存还大的值
- 设置页和「版本设置」里加了**内存占用条**：把 系统已用 / 游戏最多占用 / JVM 堆外开销
  画成一条，并标出物理内存 60% 的红线（越过容易开始换页，越玩越卡）；
  旁边有「用推荐值」，按物理内存给一个建议的堆上限
- 配置可以走**便携模式**：在启动器目录放一个 `portable.txt`，配置、账户、版本设置就都存到
  启动器旁边（拷走整个文件夹就带走设置）。切过去会把 `%APPDATA%` 里已有的配置**拷**一份过来；
  装在 `Program Files` 下写不进去会自动回退，不会因此起不来。
  设置页底部现在会写明配置到底存在哪
- **版本列表搬到了「版本」页**：原来启动页和版本页各摆一个，是同一个毛病（同一件事做两遍）。
  启动页现在只剩右上角那块启动面板，高度也压下来了；面板上多了「选择版本」，点了直接跳到版本页
- **游戏目录的选择也搬到了「版本」页**（原来在设置页）：官方启动器目录 / 启动器目录旁边 /
  浏览… 三个入口，选的目录不存在时「创建」按钮会亮起来，一键建出 `.minecraft`。
  设置页那张「游戏目录」卡片删掉了 —— 同一件事不留两个入口
- 启动面板上不再放「查看日志」：日志入口只留设置页的「启动游戏后自动打开日志窗口」开关
  （日志一般是开发者看的，玩家看的是"为什么起不来"那行提示）
- **账户头像**：从 MC 皮肤的 (8,8) 抠出脸（含帽子层）当头像，用最近邻放大所以不糊；
  离线账户显示哪张默认皮肤按 UUID 决定，跟游戏里一致。想换成自己的皮肤，
  把 png 放到配置目录的 `skins/<档案名>.png` 就行（不用登录皮肤站）
- 侧边栏去掉「账户」入口：底部那张当前档案卡片就是入口（加了小箭头提示能点）
- 启动面板贴到窗口**右下角**；版本号塞进了启动按钮（主文案大而粗、版本号小而细两行显示），
  面板上的「打开文件夹」去掉了（版本页里有）
- 侧边栏的「版本」入口去掉了 —— 启动面板上的「选择版本」就是入口，不留两个门
- 修复「浏览…」在选中官方启动器目录时点了没反应：那个目录在没装过官方启动器的机器上
  并不存在，而文件对话框拿一个不存在的目录当起点会当场退出来。现在会自动退回用户目录
- 版本列表左边支持**版本类型图标**（草方块 / 圆石 / 铁砧 / Fabric / NeoForge…）：
  把 png 放进 `assets/icons/version/` 就生效，没放就退回原来的文字徽章
- 配置文件耐折腾了：手改过 / 旧版本留下的字段**不会被静默丢掉**（以前只保留认识的键，
  下一次保存就永久没了）；写的时候先写临时文件再替换，崩在写一半不会毁掉配置；
  值会做一次纠正（`"false"` → 关、`"4096"` → 4096、颜色写错就退回默认）
- 新增独立的**游戏日志窗口**：启动后自动弹出，不挡主界面，能按 致命/错误/警告/信息/调试 各级别
  分别计数，可选显示行数、自动滚动、导出成 txt、一键结束游戏进程
- 日志窗口里**点一下级别计数就把那一级藏起来**（再点一下放出来）：不想被 INFO 刷屏时只留警告和错误，
  堆栈会跟着它上一条一起藏，启动器自己的消息不受影响
- 日志**按级别上色**：致命/错误是红的、警告是黄的、调试是灰的，出错那几行自己会跳出来
- 修复日志满屏 `<log4j:Event>`：1.12 以后游戏**控制台本来就是 XML**（Mojang 的配置里控制台用
  LegacyXMLLayout、写进 `logs/latest.log` 的才是可读格式），现在跟官方启动器一样解析回
  `[时:分:秒] [线程/级别]: 内容`，异常堆栈也一并还原
- 首页那张「游戏日志」卡片整个去掉了：状态提示和「查看日志」按钮收进启动卡片底部，
  日志统一在独立窗口里看，主界面不再重复显示一份
- 「关于」对话框加宽到 760px（原先 640 时说明文字折得太碎，几乎一屏只看得下一段）

## v0.1.8-alpha

- 新增繁体中文、日语、俄语三种界面语言（现在共五种）
- 新增「关于」：软件信息、隐私说明、版权声明、开源许可，入口在设置页
- 账户列表的当前档案改用左侧强调色竖条标示（原先是一个徽章，飘在行尾很散）

## v0.1.7-alpha

- 新增深色 / 浅色主题，可以手动切换，也可以跟随系统
- 主题色可以换：预设色板（青金石 / 钻石 / 仙人掌 / 金锭 / 泥土 / 红石）+ 自定义 RGB 滑块
- 新增「个性化」页，主题、主题色、界面语言都收在这里
- 账户列表改成卡片式：图标 + 名字 + uuid，当前档案左侧有强调色竖条，双击即可切换
- 修复版本页选中项的绿色高亮被表格切成好几段的问题
- 英文界面补全（之前有 20 多条没翻，会退回中文显示）

## v0.1.6-alpha

- 界面文字全部走文案系统，支持中英文实时切换（设置页 → 界面语言）
- 设置页内容比窗口高时可以滚动，不再出现卡片被压扁/切掉
- 账户列表改成卡片行：图标 + 名字 + uuid + 类型标签，当前账户额外标注
- 修复版本页脚注永远为空的问题
- 版本选择器的箭头、勾选框的对勾改用 SVG 图标（之前用 CSS 技巧画，Qt 不支持，出来是方块）
- 侧边栏背景色、勾选框文字底色不再串色
- 打包后的 exe 终于会加载深色主题了（之前路径解析错误，异常还被静默吞掉）

## v0.1.5-alpha

- 补上语言切换功能
- 目前还只能看界面，启动游戏没做
