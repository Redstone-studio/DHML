# 更新日志

发布时，workflow 会从这份文件里抓一节当 Release 正文，按这个顺序找：

1. 标题和版本号**完全相同**的（`## v0.1.6-alpha`）
2. `## 未发布` 或 `## Unreleased`

所以**平时改完东西，就往「未发布」里追加一行**；发版时把它改成版本号，
忘了改也没关系 —— 会自动用「未发布」那一节。

节的写法：标题必须是 `## ` 开头，内容写到下一个 `## ` 为止。
两节都找不到也不会让发版失败，只在正文里写一句提示。

---

## v0.5.6-alpha

- **改名了**：界面里叫 **Mosslight**（侧边栏左上角、关于页），Windows 那边看到的完整名是
  **Mosslight Launcher**（窗口标题、任务栏、Alt+Tab）。构建产物跟着变成
  `Mosslight-Launcher.exe` / `Mosslight-Launcher-<版本>-win64.zip`
- 版本号到 `v0.5.8-dev`
- 接上程序图标：任务栏、窗口、exe 都用自己那套图标（32/64/128/512 四档，Windows 自己挑）
- 版本列表的图标调大了一圈（38 → 44 像素）并按屏幕缩放比出图，
  高分屏（125% / 150%）下不再发软
- 版本列表左边的类型图标换成了 PCL 那套（正式版草方块 / 快照命令方块 / 远古圆石 /
  Forge 铁砧 / Fabric 线轴 / NeoForge 狐狸 / Quilt 拼布 / OptiFine / LabyMod / Cleanroom），
  源图 64×64 缩到显示尺寸几乎不重采样，比之前那批 256 的更清楚
- 版本列表左边能显示**版本类型图标**（正式版草方块、远古圆石、Forge 铁砧、Fabric 织布机、
  NeoForge 下界合金锭、Quilt 羊毛、OptiFine 望远镜、整合包箱子）；没放图的版本自动退回文字徽章
- **可以真正启动游戏了**：自动挑 Java、拼启动参数、实时显示游戏日志，随时停止
- 修复离线 UUID 算错的问题（旧值少了版本位，会让存档和服务器认不出同一个玩家）
- 首页改成**左边版本列表 + 右边启动面板**：版本下拉框去掉了（它看不见加载器类型、
  Java、能不能启动，而这些恰恰是选版本时要看的），双击版本行直接启动，
  也会记住上次启动的版本
- 侧边栏「版本」页改成**版本管理**：和首页共用同一个列表，右边显示版本路径、缺哪些库，
  可以一键补全缺失的库、打开版本文件夹
- 新增**版本独立设置**：Java、内存、额外 JVM 参数可以按版本单独定，没勾的项跟着启动器
  默认走（以后改全局默认它们会跟着变，不会被抄死的旧值卡住）。
  设置存在 `versions.json` 里，**不写进版本 JSON** —— 那个文件是安装器管的、带校验，
  改了会被当成"版本被改坏"
- 设置页的**最大内存改成滑块**（拖比点小箭头快，滑块长度本身也提示「物理内存有多大」），
  拖出来的值会吸附到 512 的档位；上限跟着物理内存走，配不出比内存还大的值
- 设置页和「版本设置」里加了**内存占用条**：把 系统已用 / 游戏最多占用 / JVM 堆外开销
  画成一条，并标出物理内存 60% 的红线（越过容易开始换页，越玩越卡）；
  旁边有「用推荐值」，按物理内存给一个建议的堆上限
- 配置可以走**便携模式**：在启动器目录放一个 `portable.txt`，配置、账户、版本设置就都存到
  启动器旁边（拷走整个文件夹就带走设置）。切过去会把 `%APPDATA%` 里已有的配置**拷**一份过来；
  装在 `Program Files` 下写不进去会自动回退，不会因此起不来。
  设置页底部现在会写明配置到底存在哪
- **版本列表搬到了「版本」页**：原来启动页和版本页各摆一个，是同一个毛病（同一件事做两遍）。
  启动页现在只剩右上角那块启动面板，高度也压下来了；面板上多了「选择版本」，点了直接跳到版本页
- **游戏目录的选择也搬到了「版本」页**（原来在设置页）：官方启动器目录 / 启动器目录旁边 /
  浏览… 三个入口，选的目录不存在时「创建」按钮会亮起来，一键建出 `.minecraft`。
  设置页那张「游戏目录」卡片删掉了 —— 同一件事不留两个入口
- 启动面板上不再放「查看日志」：日志入口只留设置页的「启动游戏后自动打开日志窗口」开关
  （日志一般是开发者看的，玩家看的是"为什么起不来"那行提示）
- **账户头像**：从 MC 皮肤的 (8,8) 抠出脸（含帽子层）当头像，用最近邻放大所以不糊；
  离线账户显示哪张默认皮肤按 UUID 决定，跟游戏里一致。想换成自己的皮肤，
  把 png 放到配置目录的 `skins/<档案名>.png` 就行（不用登录皮肤站）
- 侧边栏去掉「账户」入口：底部那张当前档案卡片就是入口（加了小箭头提示能点）
- 启动面板贴到窗口**右下角**；版本号塞进了启动按钮（主文案大而粗、版本号小而细两行显示），
  面板上的「打开文件夹」去掉了（版本页里有）
- 侧边栏的「版本」入口去掉了 —— 启动面板上的「选择版本」就是入口，不留两个门
- 修复「浏览…」在选中官方启动器目录时点了没反应：那个目录在没装过官方启动器的机器上
  并不存在，而文件对话框拿一个不存在的目录当起点会当场退出来。现在会自动退回用户目录
- 版本列表左边支持**版本类型图标**（草方块 / 圆石 / 铁砧 / Fabric / NeoForge…）：
  把 png 放进 `assets/icons/version/` 就生效，没放就退回原来的文字徽章
- 配置文件耐折腾了：手改过 / 旧版本留下的字段**不会被静默丢掉**（以前只保留认识的键，
  下一次保存就永久没了）；写的时候先写临时文件再替换，崩在写一半不会毁掉配置；
  值会做一次纠正（`"false"` → 关、`"4096"` → 4096、颜色写错就退回默认）
- 新增独立的**游戏日志窗口**：启动后自动弹出，不挡主界面，能按 致命/错误/警告/信息/调试 各级别
  分别计数，可选显示行数、自动滚动、导出成 txt、一键结束游戏进程
- 日志窗口里**点一下级别计数就把那一级藏起来**（再点一下放出来）：不想被 INFO 刷屏时只留警告和错误，
  堆栈会跟着它上一条一起藏，启动器自己的消息不受影响
- 日志**按级别上色**：致命/错误是红的、警告是黄的、调试是灰的，出错那几行自己会跳出来
- 修复日志满屏 `<log4j:Event>`：1.12 以后游戏**控制台本来就是 XML**（Mojang 的配置里控制台用
  LegacyXMLLayout、写进 `logs/latest.log` 的才是可读格式），现在跟官方启动器一样解析回
  `[时:分:秒] [线程/级别]: 内容`，异常堆栈也一并还原
- 首页那张「游戏日志」卡片整个去掉了：状态提示和「查看日志」按钮收进启动卡片底部，
  日志统一在独立窗口里看，主界面不再重复显示一份
- 「关于」对话框加宽到 760px（原先 640 时说明文字折得太碎，几乎一屏只看得下一段）

<!-- 改了东西就往这下面加一行。发版时把这一行标题改成版本号；
     忘了改也没关系 —— workflow 找不到同名版本时会自动用这一节。 -->

## v0.1.8-alpha

- 新增繁体中文、日语、俄语三种界面语言（现在共五种）
- 新增「关于」：软件信息、隐私说明、版权声明、开源许可，入口在设置页
- 账户列表的当前档案改用左侧强调色竖条标示（原先是一个徽章，飘在行尾很散）

## v0.1.7-alpha

- 新增深色 / 浅色主题，可以手动切换，也可以跟随系统
- 主题色可以换：预设色板（青金石 / 钻石 / 仙人掌 / 金锭 / 泥土 / 红石）+ 自定义 RGB 滑块
- 新增「个性化」页，主题、主题色、界面语言都收在这里
- 账户列表改成卡片式：图标 + 名字 + uuid，当前档案左侧有强调色竖条，双击即可切换
- 修复版本页选中项的绿色高亮被表格切成好几段的问题
- 英文界面补全（之前有 20 多条没翻，会退回中文显示）

## v0.1.6-alpha

- 界面文字全部走文案系统，支持中英文实时切换（设置页 → 界面语言）
- 设置页内容比窗口高时可以滚动，不再出现卡片被压扁/切掉
- 账户列表改成卡片行：图标 + 名字 + uuid + 类型标签，当前账户额外标注
- 修复版本页脚注永远为空的问题
- 版本选择器的箭头、勾选框的对勾改用 SVG 图标（之前用 CSS 技巧画，Qt 不支持，出来是方块）
- 侧边栏背景色、勾选框文字底色不再串色
- 打包后的 exe 终于会加载深色主题了（之前路径解析错误，异常还被静默吞掉）

## v0.1.5-alpha

- 补上语言切换功能
- 目前还只能看界面，启动游戏没做
