/* ==================== 模组搜索 / 详情（社区资源 → 模组）====================
   从 experiments/Downloading mod test 的搜索页 / 详情页移植过来。

   ⚠️ 实验那版把颜色全写在 setStyleSheet 里（#1b1c1f / #5ec269 / #232428 …），
   它只跑深色所以没事；这里两套主题，所以一律换成主题变量，代码里不写颜色。
   （注释里也别写出占位符本身：main_window 会扫全文找漏替换的变量。）

   命名和现有的一致：卡片容器用 ID 选择器，行内文字用 RowName / RowMeta，
   徽章复用 30-badges.qss 里的那套（Badge / BadgeAccent / BadgeWarn）。 */

/* ---------- 筛选卡：搜索框 + 三个下拉 + 按钮，当"一件事"收在一张卡里 ---------- */
#ModFilterPanel {
    background-color: @bg_card@;
    border: 1px solid @border@;
    border-radius: 10px;
}
#ModFilterPanel QLabel {
    color: @text_dim@;
    font-size: 12px;
}
#ModPanelTitle {
    color: @text_bright@;
    font-size: 15px;
    font-weight: bold;
}

/* ---------- 类型标签栏（模组 / 整合包 / 数据包 / 资源包 / 光影包）----------
   选中态用 `:checked`（代码里只 setCheckable + setChecked，不写死颜色）。
   ⚠️ 用 QPushButton：QToolButton 的文字强制居中，标签栏要左起排列。 */
#ModTypeTab {
    background-color: @bg_control@;
    border: 1px solid @border_control@;
    border-radius: 7px;
    color: @text_dim@;
    padding: 6px 14px;
    font-size: 12px;
}
#ModTypeTab:hover {
    background-color: @bg_hover@;
    color: @text_bright@;
    border-color: @border_hover@;
}
/* ⚠️ 顺序：`:checked` 写在 `:hover` **后面**，否则选中的那个悬停时会掉绿 */
#ModTypeTab:checked {
    background-color: @accent_fill@;
    border-color: @accent@;
    color: @on_accent@;
    font-weight: bold;
}
#ModTypeTab:checked:hover {
    background-color: @accent_fill_hover@;
    border-color: @accent@;
}

/* ---------- 搜索结果卡片 ---------- */
#ModCard {
    background-color: @bg_card@;
    border: 1px solid @border@;
    border-radius: 10px;
}
#ModCard:hover {
    background-color: @bg_elevated@;
    border-color: @accent@;
}
/* 卡片里的标签一律透明底：不透明会盖住卡片自己的底色和 hover 效果 */
#ModCard QLabel {
    background-color: transparent;
}
#ModCardName {
    color: @text_bright@;
    font-size: 14px;
    font-weight: bold;
}
#ModCardAuthor {
    color: @text_faint@;
    font-size: 11px;
}
#ModCardDesc {
    color: @text_dim@;
    font-size: 12px;
}
#ModCardMeta {
    color: @text_faint@;
    font-size: 11px;
}
/* 图标占位方块（图还没下来时显示 "?" 的那个） */
#ModCardIcon {
    background-color: @bg_control@;
    border: 1px solid @border@;
    border-radius: 8px;
    color: @text_faint@;
    font-size: 20px;
}

/* ---------- 项目类型徽章（模组 / 光影 / 资源包 …）----------
   加载器徽章复用 #BadgeAccent，警告类复用 #BadgeWarn（都在 30-badges.qss） */
#ModTypeBadge {
    background-color: @bg_control@;
    border: 1px solid @border_control@;
    border-radius: 4px;
    color: @text_secondary@;
    padding: 1px 6px;
    font-size: 10px;
}

/* ---------- 详情页：上面信息卡固定、下面版本列表滚 ---------- */
#ModDetailCard {
    background-color: @bg_card@;
    border: 1px solid @border@;
    border-radius: 10px;
}
#ModDetailCard QLabel {
    background-color: transparent;
}
#ModDetailName {
    color: @text_bright@;
    font-size: 18px;
    font-weight: bold;
}
#ModDetailSlug {
    color: @text_faint@;
    font-size: 12px;
}
#ModDetailMeta, #ModDetailStats {
    color: @text_dim@;
    font-size: 12px;
}
#ModDetailDesc {
    color: @text_secondary@;
    font-size: 12px;
}
#ModDetailIcon {
    background-color: @bg_control@;
    border: 1px solid @border@;
    border-radius: 10px;
    color: @text_faint@;
    font-size: 28px;
}

/* ---------- 版本列表 ----------
   分组是**可折叠**的（Fabric API 有 400 多个分组，平铺出来一眼望不到底）。
   ⚠️ 标题栏用的是 **QPushButton**（不是 QToolButton）：QToolButton 的文字
   **永远居中**（`text-align` 对它不生效），而这里要左对齐才像列表的标题行。
   下面这条 `text-align: left` 只有在 QPushButton 上才管用。
   ⚠️ 代码里还必须 `setSizePolicy(Expanding, ...)`，否则按钮会缩到只包住文字。 */
#ModVersionList, #ModVersionList > QWidget > QWidget {
    background-color: transparent;
    border: none;
}
#ModGroupHeader {
    background-color: @bg_card@;
    border: 1px solid @border@;
    border-radius: 8px;
    padding: 10px 14px;
    text-align: left;
    color: @text@;
    font-size: 13px;
    font-weight: bold;
}
#ModGroupHeader:hover {
    background-color: @bg_elevated@;
    border-color: @accent@;
    color: @accent@;
}
#ModGroupHeader:checked {
    background-color: @bg_card@;
    border-color: @border_control@;
}
/* 展开出来的内容区：底色透明，别盖住背后的页面 */
#ModGroupBody {
    background-color: transparent;
}
#ModVersionCard {
    background-color: @bg_card@;
    border: 1px solid @border@;
    border-radius: 8px;
}
#ModVersionCard QLabel {
    background-color: transparent;
}
#ModVersionName {
    color: @text@;
    font-size: 13px;
    font-weight: bold;
}
#ModVersionMeta {
    color: @text_faint@;
    font-size: 11px;
}
/* 版本类型徽章（正式版 / 测试版 / 快照）。用属性选择器选色，
   代码里只 setProperty("vtype", ...)，不写死颜色。 */
#ModVersionBadge {
    border-radius: 4px;
    padding: 1px 6px;
    font-size: 10px;
    border: 1px solid transparent;
}
#ModVersionBadge[vtype="release"] {
    background-color: @accent_bg@;
    border-color: @accent_border@;
    color: @accent_text@;
}
#ModVersionBadge[vtype="beta"] {
    background-color: @warn_bg@;
    border-color: @warn_border@;
    color: @warn@;
}
#ModVersionBadge[vtype="alpha"] {
    background-color: @danger_bg@;
    border-color: @danger_border@;
    color: @danger@;
}

/* 详情页顶部那一行状态（已选版本 / 落点提示） */
#ModPickHint {
    color: @text_faint@;
    font-size: 11px;
}
